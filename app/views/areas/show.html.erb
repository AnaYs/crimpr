
<h1><%= @area.name %></h1>
<p><%= @area.description %></p>

<h3> Current temperature in <%= @area.name %> </h3>
<%= @weather.current.temperature %>

<h3> Current other weather in <%= @area.name %> </h3>
<%= @weather.current.pressure %>
<br>
<p> Skies are <%= @weather.current.condition %> </p>
<p> Once we download the icons, this will become a little picture! Now, it's just boring text! <%= @weather.current.icon %> </p>

<p> Sun rises at <%= @weather.current.sun.rise %> </p>
<p> Sun sets at <%= @weather.current.sun.set %> </p>

<h2>Sectors</h2>
  <ul>
    <% @area.sectors.each do |sector|%>
    <li><%= link_to sector.name, area_sector_path(@area, sector) %></li>
    <% end %>
  </ul>


<br>

<h2>Pictures</h2>

<%= form_for [@area, @area.pictures.new] do |f| %>
  <p>
     <%= f.label :picture %>
     <%= f.file_field :picture %>
  </p>
  <p> <%= f.submit %> </p>
<% end %>

<% @area.pictures.each do |pic| %>
<%= pic.id %>
<%= image_tag pic.picture.url %>

<% end %>

<%= link_to "Home", areas_path, class:"btn btn-default" %>
<!-- You can't put a regular HTML-button in here or you need to append
 javascript to trigger the linking-action. Hence, you make a ERB link which you
 format as a button   -->
